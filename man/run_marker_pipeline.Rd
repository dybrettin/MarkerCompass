% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_pipeline - Copy.R, R/run_pipeline.R
\name{run_marker_pipeline}
\alias{run_marker_pipeline}
\title{Comprehensive 16S rRNA Gene Taxonomic Resolution Pipeline}
\usage{
run_marker_pipeline(
  target_genera = c("Commensalibacter", "Apilactobacillus"),
  output_dir = ".",
  db_dir = ".",
  mafft_path = "mafft",
  target_gene = "16S",
  feature_type = "rRNA",
  custom_primers = NULL,
  only_reference = TRUE,
  dereplicate_strains = TRUE,
  remove_unclassified = TRUE,
  enable_lpsn_check = TRUE,
  lpsn_db_path = "lpsn_gss.csv",
  max_contigs = 100,
  refseq_max_age = 30,
  max_tax_level = "Genus",
  n_threats = 2,
  max_scout_genera = Inf,
  keep_genomes = TRUE
)

run_marker_pipeline(
  target_genera = c("Commensalibacter", "Apilactobacillus"),
  output_dir = ".",
  db_dir = ".",
  mafft_path = "mafft",
  target_gene = "16S",
  feature_type = "rRNA",
  custom_primers = NULL,
  only_reference = TRUE,
  dereplicate_strains = TRUE,
  remove_unclassified = TRUE,
  enable_lpsn_check = TRUE,
  lpsn_db_path = "lpsn_gss.csv",
  max_contigs = 100,
  refseq_max_age = 30,
  max_tax_level = "Genus",
  n_threats = 2,
  max_scout_genera = Inf,
  keep_genomes = TRUE
)
}
\arguments{
\item{target_genera}{A character vector of genera to analyze (e.g., c("Gilliamella", "Snodgrassella")) or input a .csv file with a list of genera and all will be processed with a log file to summarize resolution of primers for each genus.}

\item{output_dir}{String. Path to the folder where results should be saved. Defaults to current working directory. Each genera targeted generates a new folder in the output directory with all output files}

\item{db_dir}{String. Path to the directory where master databases (NCBI summary, LPSN) are stored. Defaults to current working directory. Setting this to a static folder saves your laptop's bandwidth by preventing the pipeline from re-downloading the databases for every new output directory.}

\item{mafft_path}{String. Path to MAFFT executable (default: "mafft"). If MAFFT is not found, the pipeline automatically falls back to DECIPHER. Set to "" to intentionally force DECIPHER and bypass the MAFFT system check.}

\item{target_gene}{String. The specific gene to extract from the GFF file (default: "16S"). RefSeq has a standardized annotation pipeline, so if having issues download a genomes GFF file and look through it to find the spelling for your gene of interest.}

\item{feature_type}{String. The GFF feature category to filter by (e.g., "rRNA", "CDS", "gene"). Set to "ANY" to bypass this filter (default: "rRNA"). Multiple genes have 16S in the name, but only one rRNA has 16S in the name, so use this to limit your search to the correct catagory.}

\item{custom_primers}{Optional. Path to a CSV file or an R list containing custom primer sets.}

\item{only_reference}{Logical. If TRUE, only uses RefSeq/Representative genomes. Some RefSeq/Representative genomes are not validily published according to LPSN, so enable that setting as well if wanting only validly published reference species. TRUE is recommended as some genera have many poor quality genomes.}

\item{dereplicate_strains}{Logical. Keep only the best genome per strain.}

\item{remove_unclassified}{Logical. Remove unclassified strains like "sp." or "indicum".}

\item{enable_lpsn_check}{Logical. Validate names against LPSN database. HIGHLY RECOMMENDED* there are many synonym names in the refseq database which will give false negatives about a taxa being monophyletic}

\item{lpsn_db_path}{String. Path to the LPSN database CSV file. Can be downloaded at https://lpsn.dsmz.de/downloads}

\item{max_contigs}{Numeric. Maximum allowed contigs for draft genomes. RefSeq representative genomes are always kept regardless of contig #.}

\item{refseq_max_age}{Numeric. Maximum age (in days) of the local RefSeq summary file before a new one is downloaded. Set to Inf to always use the local file if it exists, or 0 to force a fresh download. Default is 30.}

\item{max_tax_level}{String. The highest taxonomic level to assess for clade resolution (e.g., "Genus", "Family", "Order", "Class", "Phylum"). Computation resources and time increase dramatically with increase taxa levels.}

\item{n_threats}{Numeric. The number of closest outgroup genera to pull full species data and align. Defaults to 2. If wanting to look at all species in a Family or higher taxonomic level, set to Inf and make sure max_scout_genera is also set to Inf}

\item{max_scout_genera}{Numeric. Maximum number of outgroup genera to fetch during the phylogenetic scout phase. Defaults to 50. Set to Inf for unlimited. A setting to tune as needed based on time and computational resources.}

\item{keep_genomes}{Logical. If TRUE (default), retains the downloaded .fasta and .gff files. If FALSE, deletes them after the run to save disk space. Unless you have a lot of extra space FALSE is recommended when going above the genus level.}
}
\value{
Generates output folders containing alignments, trees, mismatch reports, and resolution plots.

Generates output folders containing alignments, trees, mismatch reports, and resolution plots.
}
\description{
This function downloads genomes from NCBI based on targeted genera, dereplicates them, and uses .gff
coordinates to extract full-length 16S genes from .fasta files. It aligns the
sequences, performs in-silico PCR on common 16S rRNA gene regions, and generates
phylogenetic resolution metrics as well as a primer mismatch report to help with
primer and 16S rRNA region selection.

This function downloads genomes from NCBI based on targeted genera, dereplicates them, and uses .gff
coordinates to extract genes from .fasta files. It aligns the
sequences, performs in-silico PCR on gene regions based on primers inputed, and generates
phylogenetic resolution metrics as well as a primer mismatch report to help with
primer selection and taxonomic assignment analysis.
}
